name: CI
on:
  push:
    branches: [ master, task4 ]
  pull_request:
    branches: [ master ]

jobs:
  main-job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        cd task4
        git clone https://github.com/google/googletest.git
        mv googletest 3rdparty
        mkdir build
        mkdir output
        MAIN_DIR=$(pwd)
        echo ${MAIN_DIR}
        cd ../build
        cmake -DGOOGLE_TEST_DIR=googletest -DORIGIN=$(pwd) -DCMAKE_INSTALL_PREFIX=${MAIN_DIR}/output ..
        make
        make install
        
    - name: Test
      uses: akhtyamovpavel/ActionGtestReport@v0.0.9
      with:
        exec-path: /home/runner/work/TP_tech/TP_tech/task4/bin/CTestExe
        report-path: /home/runner/work/TP_tech/TP_tech/task4/test-report.tar.gz
        working-directory: /home/runner/work/TP_tech/TP_tech/task4/bin
        
